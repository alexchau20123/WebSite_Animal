<html>
<head>
    <title>会话管理</title>
    <script>
    $(document).ready(function() {
        $('#signup').click(function(evt){
            evt.preventDefault();
            $.ajax({
                url: '/auth/signup',
                type: 'POST',
                data: {
                    name: 'test2',
                    email: 'test@hkkgl.com',
                    password: 'test123',
                    wallet: 'lksdjhfoihverjfnbowei'
                },
                success: function(data){
                    if(data.ret_code === 0){
                        location.reload();
                    }
                }
            });
        });
    });
    </script>

    <UI_tag></UI_tag>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="css/canvasCSS_testBootstrap.css">
    <link rel="stylesheet" href="css/Bootstrap_customizeExtand.css">
    <script type="text/javascript" src="http://code.jquery.com/jquery.min.js"></script>


</head>
<body>
  <header></header>
  <header class="navbar bg-teal navbar-expand col-10 offset-1
  navbar-expand-xs">
    <a class="navbar-brand mr-auto" href="#">
      <img src="images/ethGame_logo.png" alt="logo">
      <p class="black">CRYPTO</p><p class="white">ANIMAL</p>
    </a>

    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link black" href="#">SIGN IN</a>
      </li>
      <li class="nav-item">
        <a class="nav-link white" href="#">MARKETPLACE</a>
      </li>
    </ul>
  </header>

<% if(isLogined){ %>
    <p>当前登录用户：<%= name %>，<a href="/auth/logout" id="logout">退出登陆</a></p>
<% }else{ %>
    <form method="POST" action="/auth/login">
        <input type="text" id="name" name="name" value="test" />
        <input type="password" id="password" name="password" value="1234" />
        <input type="submit" value="登录" id="login" />
    </form>
    <button id="signup">"一鍵註冊"</button>
<% } %>








</body>
</html>
